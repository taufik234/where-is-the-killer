{
  "name": "query-noir",
  "version": "0.1.0",
  "private": true,
  "description": "SQL Detective Game — Query Noir. Monorepo: Astro frontend + Express backend + SQLite.",
  "scripts": {
    "dev": "pnpm --parallel --filter @query-noir/backend --filter @query-noir/web dev",
    "build": "pnpm -r build",
    "start": "pnpm build && pnpm --filter @query-noir/backend start",
    "seed": "pnpm --filter @query-noir/backend seed"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "better-sqlite3": "13.0.3"
  }
}
